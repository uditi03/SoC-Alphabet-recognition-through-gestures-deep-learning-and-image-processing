# grayscale
# blur
# edge det/cascading: canny
# diluting
# eroding
# resize #aspect ratio not taken care of #interpolation = area, if shrinking or =linear/cubic(slow but best) if enlarging
# cropping  #uses array slicing